# 2026-06-09 工作日志

## OPC看板M2修复 ✅ (08:10-08:25)

### 背景
- Daryl早上询问OPC看板M2交互功能问题
- 我最初找不到项目路径（记忆索引问题）
- Kitty通过sessions_send发送了完整项目日志

### 发现的问题

#### 1. 前端未集成实时活动API ❌
**症状**: 
- Kitty昨晚修复的`/api/agent-activities` API后端正常
- 但前端`index.html`完全没调用，还在用旧的`/api/agents`

**根因**: 
- 前后端沟通断层
- Kitty修复了后端，没通知前端需要改

#### 2. 刷新频率不一致 ⚠️
**发现**:
- COORD.md写"15秒刷新"
- 实际代码是**5分钟**（300000ms）
- Kitty日志说"30秒刷新"

#### 3. BrokenPipe异常污染日志 🔴
**日志**:
```
BrokenPipeError: [Errno 32] Broken pipe
  at do_GET() -> self.wfile.write()
```
**影响**: 后端日志堆满异常栈

#### 4. 重复API定义 🐛
`/api/agent-activities`在server.py中定义了两次（line 350和360）

### 修复内容

#### Fix #1: 前端集成实时活动API
**文件**: `index.html:440-454`
**改动**:
```javascript
// 旧代码
const res = await fetch('/api/agents');

// 新代码
const res = await fetch('/api/agent-activities');
// 转换新API格式到旧格式兼容
this._agents = Object.entries(data).map(([name, info]) => {
  const sessions = info.sessions || [];
  const latestSession = sessions[0] || {};
  return {
    name,
    status: latestSession.age === 'just now' ? 'online' : 'idle',
    model: latestSession.model,
    tokens: latestSession.tokens,
    lastActive: latestSession.age,
    sessions // 保留完整sessions
  };
});
```

#### Fix #2: 统一刷新频率到15秒
**文件**: `index.html:329`
**改动**:
```javascript
// 旧代码
setInterval(() => this.autoRefresh(), 300000) // 5分钟

// 新代码  
setInterval(() => this.autoRefresh(), 15000) // 15秒（统一到COORD.md）
```

#### Fix #3: 捕获BrokenPipe异常
**文件**: `server.py`
**修复点**: 
- `/api/agents` (line 338)
- `/api/artifacts` (line 348)
- `/api/agent-activities` (line 358, 删除重复定义)
- `/api/cost` (line 380)
- `/api/sandbox` (line 395)

**模式**:
```python
try:
    self.wfile.write(json.dumps(data).encode())
except BrokenPipeError:
    pass  # 客户端断开，静默处理
except Exception as e:
    print(f"⚠️ Error writing response: {e}")
```

#### Fix #4: 删除重复API定义
移除`/api/agent-activities`的第二个定义块

### 验证结果

✅ **服务重启成功**:
- 旧进程73086已停止
- 新进程10117启动（8090端口）
- ngrok tunnel已启动

✅ **API测试通过**:
```bash
curl http://localhost:8090/api/agent-activities
# 返回4个Agent的实时sessions，数据正确
```

✅ **日志干净**:
- 不再有BrokenPipe异常栈
- 只记录正常请求

### 待验证（需要Daryl测试）
- [ ] 浏览器访问http://localhost:8090看到最新Agent状态
- [ ] 四宫格自动15秒刷新
- [ ] 编辑任务功能正常工作
- [ ] DM通知功能（如果启用）

### 总用时
- 诊断: 10分钟
- 修复: 5分钟
- 验证: 3分钟
- **总计**: 18分钟

### 经验教训
1. **记忆索引问题** — 我找不到项目路径，证明memory/索引确实需要改进
2. **前后端协调** — Kitty修复后端API后，应该立即同步前端需要改的地方
3. **文档与代码不一致** — COORD.md说15秒，代码写5分钟，要统一
4. **异常处理要完整** — 所有网络写入都要捕获BrokenPipe

### 下一步
- 等Daryl验收M2完整功能
- 如果通过，更新`memory/active.md`将M2标记为✅
- 记录到`memory/lessons.md`
