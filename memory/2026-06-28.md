# 2026-06-28 记忆

## 视频分析交互Workflow v1.1.1 Debug

### Daryl 四项 bug 修复（全量完成）
1. **🔇 视频无声音**: you-get 下载B站视频时只选 video stream (dash-flv480-AVC)，无音频流。修复：改为下载全部dash-flv480格式 → ffprobe检测分离的video+audio部分 → ffmpeg合并。yt-dlp 同步加 `-f bestvideo+bestaudio/best`
2. **📜 对白模块无滚动条**: `.transcript-col` 缺少 flex 约束，内部 `.transcript-list` 无高度参考无法overflow。修复：加 `flex:1; min-height:0; display:flex; flex-direction:column`
3. **📊 进度条改为%显示**: 原为 indeterminate 动画（移动闪烁"进行中"）→ 改为 server 端 stage-based 进度上报（5%→15%→25%→50%→60%→95%→100%），前端显示百分比文字+进度条宽度
4. **💰 商业数据URL获取**: B站 API 被后端（境外IP）封锁 → 新增前端直连 B站 API fallback（利用用户浏览器国内IP），数据增加弹幕字段

### 技术要点
- you-get 下载B站：dash-flv480-AVC只含视频，dash-flv480含分离的video+audio
- ffprobe检测流类型：`-select_streams v:0/a:0 -show_entries stream=codec_type`
- 滚动链：flex容器必须有`min-height:0`才能让子元素overflow生效
- 前后端双轨道commerce：前端B站API优先（国内IP），后端兜底

### 产物
- video_analyzer_app/index.html（修改：CSS+JS+HTML）
- video_analyzer_app/server.py（修改：_run_transcribe进度+下载器音频合并+commerce）
- VERSION → 1.1.1

### v1.1.1 第二轮Debug（下午）
Daryl 反馈 4 项仍不对：

1. **商业数据**: 之前只加了B站前端fallback，抖音和YT没处理 → 新增 `fetchDouyinCommerceFE()`（手机UA抓页面JSON）+ `fetchYoutubeCommerceFE()`（oembed+页面正则），后端同步加了抖音正则抓取
2. **视频无声音**: 检查发现4个B站旧视频无音频流。修复：音频检测后若无音频→跳过继续下一phase（yt-dlp兜底）而非直接使用无声视频
3. **滚动条颜色**: 从 `rgba(255,255,255,0.12)` 改为 `0.06`（与深色背景融合），hover从 `0.25`→`0.15`
4. **进度条不显示%**: 根因分析→进度%现在直接显示在状态栏文字中（`⏳ 分析中 · 12s · 35% · Gemini...`），同时保留底部横条。添加客户端时间估算fallback（server数据优先）

### v1.1.2 · 抖音下载修复
- **根因**: douyin.com 检测海外IP（越南）→ 重定向到首页而非视频页
- **发现**: iesdouyin.com/share/video/{id}/ 海外可正常访问，video_id 直接嵌入页面HTML
- **修复**: douyin_downloader.py V4 重写
  - 主路径：urllib抓取iesdouyin页面→正则提取video_id→curl下载playwm视频流（秒级完成，无需Playwright）
  - Playwright降级为fallback
  - 前端 resolveDouyinUrl()：v.douyin.com→提取video ID→构造iesdouyin.com URL
- **测试**: `v.douyin.com/12S56LK_ESQ` → 1080p 19.2MB 含音频，5秒完成
- **版本**: 1.1.1 → 1.1.2

### 第一阶段归档
- Daryl 指令: 归档 v1.1.2 全量 → Workflow/后端/前端/隧道/开发经验
- 产物: `video_analyzer_app/ARCHIVE_REPORT_v1.1.2.md`
- 本地: localhost:8777 | 公网: unwhispering-imani-digitately.ngrok-free.dev
- Ngrok 隧道配置: ~/Library/Application Support/ngrok/ngrok.yml
  - bryson-app (8777) / opc-dashboard (8765)
- Git → GitHub: DarylChiu/opc-workspace.git, branch main
