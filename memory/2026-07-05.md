# 2026-07-05 日记

## Daryl 活动
- **10:17** Daryl 指令：继续"雅思陪练助手v2.0"项目验收
- **10:20** 重启服务器（DEBUG_MODE=1），完成验收准备

## Agent 活动
- **Bryson**: 重启服务器启用 DEBUG_MODE，验证所有 debug 端点 + ngrok 公网可访问
- **10:40** 更新 `memory/active.md`：视频分析交互Workflow 条目从 10fpsVision(旧)→V3 Gemini原生视频(当前)，标记为已验收；合并重复的「下一阶段」section；清理过时描述
- **15:00** 修复 IELTS v2.0 音频采集 Bug：VAD 校准域不匹配(AnalyserNode频域 vs PCM时域) + ScriptProcessor 无下游连接 + iPhone 低电平三重叠加导致 0 turns。修复：统一 PCM 校准 + 零增益保活 + 静音滚动缓冲超时强制 flush + 手动发送增强
- **15:28** IELTS v2.0 延时优化 V1 + V2:
  1. ASR: PCM→FLAC 压缩 + useEnhanced=False
  2. TTS: 流式逐句发送，首块音频回调
  3. LLM: max_tokens 200→80，上下文 17 条消息
  4. 终止评估: asyncio.Task cancel + finally 块 auto-eval 兜底
  5. 重复提问: 系统 prompt 加主题清单追踪 + 禁止重复规则
  6. 实测: E2E 4768ms avg ✅ 达成 5000ms 目标
# 2026-07-05 日记

## Daryl 活动
- **10:17** Daryl 指令：继续"雅思陪练助手v2.0"项目验收
- **10:20** 重启服务器（DEBUG_MODE=1），完成验收准备

## Agent 活动
- **Bryson**: 重启服务器启用 DEBUG_MODE，验证所有 debug 端点 + ngrok 公网可访问
- **10:40** 更新 `memory/active.md`：视频分析交互Workflow 条目从 10fpsVision(旧)→V3 Gemini原生视频(当前)，标记为已验收；合并重复的「下一阶段」section；清理过时描述
- **15:00** 修复 IELTS v2.0 音频采集 Bug：VAD 校准域不匹配(AnalyserNode频域 vs PCM时域) + ScriptProcessor 无下游连接 + iPhone 低电平三重叠加导致 0 turns。修复：统一 PCM 校准 + 零增益保活 + 静音滚动缓冲超时强制 flush + 手动发送增强
- **15:28** IELTS v2.0 延时优化 V1 + V2:
  1. ASR: PCM→FLAC 压缩 + useEnhanced=False
  2. TTS: 流式逐句发送，首块音频回调
  3. LLM: max_tokens 200→80，上下文 17 条消息
  4. 终止评估: asyncio.Task cancel + finally 块 auto-eval 兜底
  5. 重复提问: 系统 prompt 加主题清单追踪 + 禁止重复规则
  6. 实测: E2E 4768ms avg ✅ 达成 5000ms 目标
- **16:18** 代码审查发现 `server.py` tuple unpacking bug + Debug API 路由缺失
- **16:20-17:00** 修复: STT bug + Debug 模块恢复 + 管线2 初次接线
- **17:00-18:00** 多轮测试迭代:
  - 管线1: prompt 深度优化(广度→深度追问) + max_tokens/context 提升
  - 管线2: 三次修复(Cherry→Ethan voice, turn_detection 移除, 音频一次性发送)仍失败
  - 最终错误: WebSocket frame 278KB > 256KB 限制
- **18:15-18:30** 延时调研: 撰写主流方案对比报告(3种架构/组件级优化/推荐优先级)
- **18:32** 管线2 最后一次测试, 音频识别仍失败
- **18:42 Daryl 决策**:
  - 管线1-IELST Part1 验收 70% ✅（延时待优化）
  - Debug 模块验收 90% ✅
  - 管线2 暂停, 剩余问题转入 M2 Stage
  - M2 核心目标: 延时降至 3000ms 以下
