# 2026-08-13 日记

## 09:33 · OPC 看板公网隧道状态（Daryl 验收 Self 成果）

- Daryl 要最新公网隧道地址验收 Self 成果
- **serveo 固定域名仍 502**（8/12 服务端故障未恢复），watchdog 每 5 分钟重试回切
- 当前可用入口 = cloudflared 临时隧道
- ⚠️ **隧道地址两次变更**：09:25 `phil-highly-threshold-extend.trycloudflare.com` → 09:30 连接终止（导致 Daryl 访问报 Error 1033）→ 09:30 重启后新地址 `arcade-interpreted-nevertheless-radios.trycloudflare.com`（实测 200）

## ⚠️ 教训 · serveo 域名纠正（Daryl 第二次纠正）

- **错误**：我在聊天里说 serveo 固定域名是 `opc-darylchiu.serveo.net`
- **正确**：`https://opc-darylchiu.serveousercontent.com`（serveo.net 是 SSH 主机名，公网 URL 是 serveousercontent.com 域名）
- **Daryl 已纠正两次**，必须记住：serveo 公网 URL 一律用 `*.serveousercontent.com`，脚本 watchdog.sh 里 TUNNEL_URL 本来就是对的，是我口头说错
- 教训：报 URL 前先查脚本/文档中的权威值（watchdog.sh L14），不要凭印象

## 09:35 · watchdog 修复

- watchdog.sh L69 grep 读 cf.log 报 "Binary file matches"（日志含二进制字符）→ 加 `-a` 参数修复
- tunnel_url.txt 已更新为当前可用 cloudflared 地址

## 09:40 · Error 1033 根因复盘（Daryl 质疑「是不是 localhost 掉了」）

- **Daryl 质疑正确，我第一版诊断不精确**：我说隧道「不稳定」，实际根因是 watchdog 自杀式换 URL
- **localhost:8765 从未掉过**（实锤）：node server.js PID 3587 自 8/7 20:34 连续运行 6 天未重启；watchdog 每 5 分钟本地检查全过；多次 curl 200
- **真实根因**：serveo 宕机期间，watchdog 每次检测到 serveo 502 → pkill 现有**健康**的 CF 隧道 → 重建新随机 URL。09:25 phil→09:30 被杀换 arcade→09:35 又被杀换 singer。我 09:26 给 Daryl 的 URL 09:30 就被 watchdog 杀了 → Error 1033
- **修复**：watchdog.sh CF 回退分支改为「已有健康 CF 隧道则保留不重建」（exit 0），只在不存在的才新建；已语法校验
- **教训**：报 URL 前查脚本逻辑；「不稳定」是表象，要追到机制（watchdog 每 5 分钟 pkill）
- 当前有效 URL：https://singer-mechanisms-rolled-geographical.trycloudflare.com（200）

## 成本扫描（Balance 例行报告）
- 今日: $0.75 | 本月: $22.70 | 全量: $148.49
- 看板 /api/costs 与台账一致（148.49 vs 148.4888），无需改动接口
