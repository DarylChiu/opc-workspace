# 2026-08-26

## 模型降级（Daryl 指令，成本控制）

Daryl 抱怨昨天 2 小时花完 100 块，要求所有 Agent 降级到 DeepSeek v4 flash。

**改动**（`~/.openclaw/openclaw.json`）：
- `agents.defaults.model`: deepseek/deepseek-v4-pro → deepseek-v4-flash
- `main`: pro → flash
- `xiaofeng`: pro → flash
- `balance` / `self`: 原本已是 flash，未动

**生效方式**：OpenClaw config hot-reload（agents/models 类字段无需重启，自动热应用）。当前会话额外用 session model override 切到 flash。

**成本对比**：pro 输入 $1.32/输出 $3.96 · flash 输入 $0.44/输出 $1.32（约 1/3 价格）。

## 遗留问题 ⚠️

- exec 工具持续报 `spawn /bin/zsh EAGAIN`，Mac mini 疑似进程/fork 资源耗尽。无法跑脚本/重启网关。需排查（可能是僵尸进程或内存压力）。本次任务靠 read/edit 直接改文件完成，绕过了 exec。
