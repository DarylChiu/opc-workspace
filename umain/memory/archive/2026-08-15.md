# 2026-08-15

## serveo 隧道恢复排查（Daryl 报障）
- Daryl：Serveo 是不是又能用了？能看到「Agent状态和任务」但看不到「产物与预览」
- 排查结论：
  - serveo 固定域名 opc-darylchiu.serveousercontent.com 服务端已恢复（200，8/12 故障解除），autossh 正常
  - **根因**：serveo 免费版新增「Serveo Browser Warning」确认页（匿名连接防钓鱼）→ 点 Continue 种 cookie `serveo-skip-browser-warning=true` → 带 cookie 的 /api/* 全部 502（无 cookie 直连 200）
  - Agent 卡片是静态 HTML 所以能显示；产物列表依赖 /api/artifacts 被 cookie 机制搞挂，停在 Loading
- 验证：本地 8765 全正常（37 产物、无 JS 错误、预览可点）；cloudflared 备用隧道 endorsed-calgary-revolution-vip.trycloudflare.com 浏览器实测全正常
- 方案：短期用 CF 隧道；长期 A=serveo 付费去警告页 / B=Cloudflare Named Tunnel 固定域名（免费，推荐，待 Daryl 确认）

## 成本扫描（Balance cron 例行）
- 今日 $0.96 | 本月 $24.27 | 全量 $150.06，已写入 cost_daily.json，看板一致
