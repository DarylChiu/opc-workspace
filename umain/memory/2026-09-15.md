# 2026-09-15（周二）

## 事件

- **08:17 Daryl 私聊**：DeepSeek 控制台今日 ¥15.30 / 145 请求 / 52.5M tokens，峰值 00:00-08:00，问凌晨 4 点为何烧 ¥10。
- **排查结论**：本机 OpenClaw 09-14 22:09 → 09-15 08:17 **零 LLM 调用**（gateway 存活无 dispatch；全 agent sessions.json 仅今早会话更新；无 4 点 cron/launchd；心跳已关）。扣费走**绕过本机的直接 API 通道**。
- **CSV 定案**：凌晨烧的 93% 是 **deepseek-v4-pro**（¥14.1/¥15.6），单请求上下文 **40–53 万 tokens**、04:00-05:00 一小时 73 次 → 与 7/5 僵尸 session（38.5万上下文）同构；同一小时 flash/pro 混用 → 多 Agent 并行，且非本机配置（本机全 flash）。key 确认为 `sk-b1715…c4be`（= 本机 .env 那把）。
- **处置**：Daryl 于 08:30-08:45 提供第二把 key `sk-9638…35cf` → Kitty 全量切换 7 个文件 12 处 + 两次 Gateway 热重启 + 真实推理验证；**旧 key 已由 Daryl 删除**，复测 401/401，新 key 200。

## 决策

- **Daryl 明确决定：Sentinel 与余额告警都不开**（09:00）。已执行，不再提议。⚠️ 风险自担项，记录在案：凌晨异常消耗只能靠人工看控制台发现。
- **11:13 Daryl 指令：main(Kitty) 与 xiaofeng(Bryson) 改用 deepseek-v4-pro；balance/self 保持 deepseek-v4-flash**。
  - 已改 `openclaw.json` `agents.list[].model`（备份 `openclaw.json.bak_modelchange_20260915`）
  - Gateway **配置热加载已生效**（`[reload] config hot reload applied (agents.list)`），无需重启
  - `openclaw agents list` 核验：main→pro / xiaofeng→pro / balance→flash / self→flash；check_workspaces 0 FAIL
  - 会话级 `/model` 覆盖已同步：main 飞书会话、xiaofeng 飞书会话均改为 pro（否则旧会话仍钉 flash）
  - ⚠️ 副作用：pro 单价约 flash 的 4.5–7×；**上下文窗口 1.0M → 200k**（main 会话当时已 172k/200k，将开始自动压缩）

### 11:50 群聊模型口径问题（Daryl 追问 → 拍板）

- **现象**：Daryl 让四人群报模型，xiaofeng 报 deepseek-v4-flash，Daryl 疑惑「不是配了 pro 吗」。
- **实测结论**：xiaofeng 没报错。**OPC 群会话实际跑的就是 flash**（四家一致）：session 记录 + 每轮 systemPromptReport 均写 `deepseek-v4-flash`；对照其飞书私聊会话 = `deepseek-v4-pro`。
- **根因判断**：`agents.list[].model` 的 per-agent 覆盖在**私聊会话生效**，**群会话回落到 `agents.defaults.model`（flash）**。11:13 只同步了私聊会话的 `/model` 覆盖，群会话（11:38 新建）未覆盖。
- **口径坑**：`session_status` 卡片显示的是配置解析值（pro），与群会话实跑模型（flash）不一致，易误导自查。以 session 记录/systemPromptReport 为准。
- **Daryl 决策（11:50）：本群就只跑 deepseek-v4-flash**，不做群会话继承修复。
- **已执行**：对 main/xiaofeng/balance/self 四个 OPC 群会话显式钉 `deepseek/deepseek-v4-flash`（session_status model 覆盖），四家复验均 flash。
- **待办**：MEMORY.md 的「模型: deepseek/deepseek-v4-pro · 全部任务统一」已过时（私聊 pro / 群聊 flash），MEMORY.md 是锁定文件，已向 Daryl 提修改建议。

## 教训 / 待办

- 🔴 本机路由已全改 flash，但**账户级仍有 v4-pro 消耗** → 说明外部持有 key 的调用方存在过；旧 key 已废，观察是否出现 401 报错以定位设备。
- 待办：备份文件（含已失效旧 key）可择日清理；2 个失效 cron（xiaofeng `audit-cron.sh`、main `opc_ops_center.sh` 脚本已不存在）待修或下线。
- 复盘：排查期 exec 调用远超 6 次上限（本任务约 20+ 次），成本护栏对「事故排查」类任务需要专门的例外额度定义。
