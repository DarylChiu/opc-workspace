# 2026-08-27 日记

## 🔴 P0 成本泄漏事故（持续跟踪）

### 背景
- Daryl 昨天充值 ¥150，无产出即被烧光（现余额仅 ¥34.89）
- Daryl 指示：①所有可能花 API 成本的服务全关 ②图像识别优先本地 OCR，禁用 openrouter/deepseek 图像识别

### 排查结论（证据链）
1. **4 个 cron 实际仍 enabled**（昨晚声称"cron 全停"是假的）：
   - balance 每日记忆归档 23:59 / self 每日记忆归档 23:59 / self methodology 备份 23:59 / balance 成本扫描 23:45
   - 昨晚 23:45/23:59 全部触发 → 402 Insufficient Balance（那时余额已 0）
   - **已全部 disable** ✓
2. **IELTS 服务直连 DeepSeek**：`ielts_tutor/backend/server.py` 三处调用 `api.deepseek.com`，与 OpenClaw **共用同一个 DEEPSEEK_API_KEY**（env 继承）→ 已 kill + launchd 禁用
3. **video_editor_mvp** 用 OPENROUTER_API_KEY / GEMINI_API_KEY → 已 kill + launchd 禁用
4. **searxng** 有 launchd 任务 `ai.searxng.local` + keepalive.sh while 循环反复复活 → bootout + plist 禁用 + 进程树杀净
5. **账本对不上**：OpenClaw 侧昨天可见消耗仅 ~$0.1，但 DeepSeek 余额 150→0。**真正大头不在 OpenClaw 会话里** → 待 Daryl 查 platform.deepseek.com 用量页定位
6. 心跳：默认已 0m 关闭 ✓；无僵尸子代理/后台进程 ✓

### 已关停清单（9 个 launchd 自启项 → .disabled）
- ai.openclaw.ielts / ai.openclaw.videoeditor / ai.openclaw.ngrok / ai.openclaw.drive-watchdog / ai.searxng.local / ai.gosh.console / ai.gosh.feishu-bot / com.opc.evolution.distill / com.balance.cloudflared-8770

### 保留运行（零 API 成本，纯本地）
- expense_mvp (867, 端口8770 相关) · opc-dashboard (930, 8765)

### 新政策（Daryl 指令）
- **图像识别 = 本地 OCR 优先**（tesseract 已确认可用 /opt/homebrew/bin/tesseract）
- 禁用 openrouter / deepseek 的图像识别能力
- 待办：提议写入 MEMORY.md（需 Daryl 批准）

### 待 Daryl
- 查 DeepSeek 用量页（platform.deepseek.com）→ 按天 token 明细 → 定位真正烧钱源
- 确认 IELTS/video_editor/searxng 后续处置（换独立 key？重开？）
