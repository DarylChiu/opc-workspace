# 2026-07-07 日记

## 07:44 · Daryl 通知继续深度集成
- Daryl: "Gateway已重启，你可以继续昨晚深度集成的任务了"
- 确认 Sentinel 插件已加载（10 个插件之一，hookCount: 2）

## 07:45-07:50 · Sentinel 深度集成
### 已完成
- **配置完善**: 添加完整 sentinel config（阈值、路径黑白名单、夜间降级、超时设置）到 openclaw.json
- **plugins.allow**: 添加 "sentinel" 到白名单
- **AGENTS.md 更新**: 添加 Sentinel 插件说明（L0-L4 合规节）
- **active.md 更新**: 添加 Sentinel 部署状态条目
- **验证**: 插件 loaded, activated, hooks 注册成功（before_tool_call + session_end）

### Gateway 状态
- 配置变更已检测：`config change detected; evaluating reload (plugins.entries.sentinel.config, plugins.allow)`
- Gateway 推迟重启：等待当前 session 完成（`deferring until 2 operation(s), 1 reply(ies), 1 embedded run(s), 1 task run(s) complete`）

### 待 Gateway 重启后验证
- [ ] P0 路径阻断测试 (.env, *.secret)
- [ ] P1 requireApproval 测试 (package.json 修改)
- [ ] 累积阈值触发测试 (write > 20, dirs > 3)
- [ ] 审计日志生成验证
- [ ] 夜间降级逻辑验证（23:00-08:00）

## 后续任务
- [ ] pre-op.sh 对齐新边界（添加 Sentinel 协同说明）
- [ ] OPC 看板 M3 剩余: 成本面板项目拆分 + 产物项目分组 (30%)
- [ ] OPC 看板 M2.3 合规仪表板接入
- [ ] Sentinel 试运行 1 周 → 指标评估 → M2 迭代

## 09:00-09:28 · OPC看板 M3 项目总线开发
### 已完成
- **项目总线面板**: 新 nav 按钮 + HTML 面板 + 完整 JS 渲染逻辑
- **数据聚合**: 调用 `/api/costs/projects` + `/api/artifacts?group=project` + `/api/agents/projects`
- **交互功能**: 项目卡片展示(里程碑/产物/成本)、展开详情(里程碑列表+成本明细+产物清单)、跨面板快捷链接
- **侧边栏 badge**: 项目总线计数刷新
- **Commit**: `8809c5d` M3: 项目总线面板 — 跨项目深度集成 (212 insertions)
- **M3 进度**: 30% → 70%

### Gateway 问题排查
- 之前加的 `plugins.allow: ["sentinel"]` 导致 Gateway 重启后只加载 2 个插件
- Daryl 禁用 Sentinel 后 Gateway 无法正常启动
- **根因**: plugins.allow 限制了非 bundled 插件加载
- **修复**: Daryl 手工清除了 plugins.allow 配置

### Balance Feishu ID 修复
- Balance 的 USER.md 缺少 Daryl 的 Balance 通道 Feishu ID
- 已添加 `ou_0d7a57b9a531823aa1edee6874dcbb34` 到 Balance 的 USER.md
- 已添加 OPC 看板访问信息到 Balance 的 MEMORY.md

## 09:35-09:45 · M3 项目总线完成 100%
### 完成项目
- **项目总线面板**: 聚合 costs/projects + artifacts?group=project + agents/projects
- **成本面板项目拆分**: `/api/costs/projects` 面板，按项目汇总 tokens/cost/sessions，支持模型明细展开
- **产物面板项目分组**: 「按项目分组」切换按钮，显示项目名+文件数，支持 agent 过滤联动
- **Commits**: `8809c5d` (项目总线) + `690decf` (成本+产物)
- **M3 进度**: 30% → 100% ✅

## 全天 · 隔壁隧道稳定性问题（heartbeat 会话处理）
- 今天 tunnel 多次挂掉（~10+ 次），OPC Dashboard 服务进程也多次异常退出
- **根因发现**: 重启时旧 cloudflared 进程未清干净 → 多个 cloudflared 竞争同一端口 → tunnel 返回 000/502
- **修复**: 重启前必须 `pkill -9 -f cloudflared` 彻底清理，再启单一进程 → 稳定性显著提升（3.5h+ 连续运行）
- **待办**: 建议为 OPC Dashboard + tunnel 配 LaunchAgent 自动重启，根治临时隧道不稳定（需 Daryl 确认方向）

## 21:50 · Crontab 手动安装（Daryl 指令）✅
- Daryl 将「Crontab 手动安装」移至 In Progress（无描述）
- 结合全天隧道痛点 → 判断需求为装看门狗自动恢复 OPC Dashboard + tunnel
- **产物**: `opc-dashboard/scripts/watchdog.sh`
  - 检本地服务（localhost:8765）异常 → manage.sh restart
  - 检隧道异常 → pkill -9 彻底清理 + 重建单一 cloudflared + 自动写回新 URL
  - 日志滚动保留 500 行
- **crontab 条目**: `*/5 * * * *` 每 5 分钟执行
- 旧 crontab 已备份到 /tmp/crontab_backup_*.txt
- Commit `6be5c98`
- 以后无需手动重启，看门狗自动兑现
