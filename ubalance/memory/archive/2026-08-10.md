
## 🕑 14:30 IT演示实例：v0.2.0快照跑8771 + trycloudflare隧道
- **触发**: Daryl 想让 IT 同事看 OCR/分类功能，要求「v0.3.0（无3节点、仅OCR v2.0+分类器 v2.0）」跑 8771 + 隧道
- **关键发现**: v0.3.0修复版(8/8)和v0.4.0(8/9)均未git提交！git里expense_mvp最后提交=8/5 v0.2.0(8e5de04)；v0.3.0无独立备份（backup-v0.2.0是修复前）
- **执行**: git archive 8e5de04 检出到 /tmp/expense_mvp_v03_demo → PORT=8771 启动 → 冒烟测试通过（业务招待费 2,666,000₫ 置信度0.9）→ cloudflared 隧道 https://later-filled-teenage-appropriate.trycloudflare.com（外网200）
- **注意**: v0.2.0上传API走JSON/base64协议，multipart会崩（UnicodeDecodeError）；前端演示无影响
- **风险提示**: 工作区脏，v0.3.0/v0.4.0代码未入git，T0铁律数字资产保护风险 → 建议尽快提交
- **模型**: deepseek-v4-flash

## 🕒 14:32-14:45 Daryl 追问 5 张发票识别问题 → 定位+修复
- **Daryl 反馈**: 8771 上传5张发票，3张供应商识别不出、1张日期识别不出、无发票号和VAT独立识别码
- **根因1**: 8771 之前跑的是 git 8/5 快照（v0.2.0），不含 8/8 v0.3.0 修复（供应商正则/日期格式/发票号全链路都是修复清单 P0 项）
- **根因2**: Mã CQT（VAT独立识别码）16字段清单里本来就没有，属缺失字段 → 新增提取
- **修复**（classifier.py，工作区）:
  1. 发票号正则兼容 OCR 误识别 "sé"(Số) 变体
  2. 新增 cqt_code 字段 + 3种格式兼容（MCQT: / Mã CQT: / Mã CQT(Code): / Mã của cơ quan thuế:）
  3. 踩坑：`\(?[Cc]ode\)?` 需整体包成 `(?:\(?[Cc]ode\)?)?` 否则 CQT: 直接跟冒号时失配
- **部署坑**: pkill -f 匹配不到 command 只有 "server.py" 的进程（路径不匹配），导致旧进程占端口、新代码没生效，需 kill -9 + 确认 lsof
- **结果**: 5/5 全识别成功（供应商/日期/发票号/序列号/CQT），回归测试全绿（15/15 + 集成）
- **8771 现跑**: 工作区代码 v4.0.0（含 v0.3.0 全部修复 + v0.4.0），PID 68532
- **模型**: deepseek-v4-flash

## 🕓 15:00-15:20 v0.4.0 前端交互修复（Daryl 反馈三步流水线体验）
- **Daryl 反馈**: ①"未分组费用"卡片点不开详情 ②OCR 16字段没地方看 ③识别分类错了不能改
- **根因**: 卡片只绑了 ondblclick（双击）无提示；openDetail 弹层只渲染单据+预审状态，没渲染 OCR 字段；详情弹层只读，编辑功能只在审核表格页有
- **修复** (frontend/index.html):
  1. 卡片 onclick 单击打开详情（保留双击兼容，cursor:pointer + title 提示）
  2. 详情弹层新增 OCR 16+ 字段表格（发票类型/日期/号码/序列/供应商/税号/购买方/税前/VAT/合计/税率/支付/币种/住宿日期/补贴/置信度）
  3. 详情弹层新增"✏️ 修正识别"编辑表单（费用类型下拉+金额三值）→ PUT /api/review/expenses/{id} 保存并自动重审
- **验证**: JS语法通过；详情API返回16字段✅；改类型"业务招待费→办公费"保存成功+自动重审 manual_review ✅
- **8770+8771 前端均已生效**（静态文件即时读取，无需重启服务）
- **模型**: deepseek-v4-flash

## 🕓 15:25 Daryl 要求清空演示数据 + 税率显示修复
- **清空 8771 演示库**: 44张未分组 + 1张池中（45张今日测试数据）全部删除（expenses/invoices/documents/review_decisions/orders 等表 + uploads/vouchers 文件目录），验证 0/0/0/0 ✅；8770 生产库 34 张（含 16 张已凭证真实数据）不动
- **税率显示 bug 修复**: 数据库 vat_rates 存小数（0.08=8%，提取时 int/100），前端直接显示成 "0.08%"；新增 fmtRate() 显示时 ×100 → "8%"，兼容 vat_rate 直接存 10 的情况；8770+8771 前端均已生效
- **模型**: deepseek-v4-flash

## 🕓 23:45 每日成本扫描（cron 超时保护模式）
- **数据**: 今日 $0.2427 | 本月 $20.2418 | 全量 $146.0284 | Token 1.65B | 调用 24,509 次
- **扫描**: full_cost_scan.py 全量 3692 个 .jsonl，新增 277 条；兜底文件已写入 opc-dashboard/data/cost_daily.json
- **Kitty 同步**: ⚠️已降级写文件，Kitty未确认（session_send 30s 超时，runId 5c82002b）
- **API 验证**: http://localhost:8765/api/costs ✅ 正常，_source=balance-ledger 权威台账已生效，_liveScan 交叉引用正常
- **模型**: deepseek-v4-flash
