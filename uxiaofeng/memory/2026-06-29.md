# 2026-06-29 日记

## 管线2 Qwen-Omni 接入完成 🎉
- API Key 踩坑：Chat 消息手打错一个字母(P→Q)，CSV 文件 key 才是正确的
- 端点问题：国际站 workspace key 需用 `ws-spzk3f7tb6hcawwn.cn-beijing.maas.aliyuncs.com` 而非 `dashscope.aliyuncs.com`
- sim→连接成功 → session.created → text+audio双向流测试通过
- 440Hz正弦波测试失败（非人声），英文字符输入测试成功
- Model: qwen3.5-omni-flash-realtime（plus 需百炼白名单）

## Phase 3/4 并行开发完成
- 3.1 评估引擎增强：realtime 管线会话结束后触发 DeepSeek 四维评分
- 3.3 HTML 报告生成：generate_report 集成入双管线
- 4.1 会话持久化：SQLite (sessions/transcripts/evaluations/errors)，PersistentSessionManager
- 4.2 成本仪表板：/api/costs + /api/history + /api/weaknesses + 前端实时显示

## OPC 看板接入
- Kitty 通知：OPC 看板 v1.1.1 新增 Agent 小任务上报
- 已接入：实时小任务 POST /api/report，项目里程碑同步
- scripts/report_tasks.sh 快捷脚本

## 前端 UI 更新
- 管线标签从"管线1/管线2"改为用户友好命名
- 新增引擎选择器（链式流式—STT→DeepSeek→TTS / Audio Realtime—Qwen 3.5-Omni）
- pipelineLabel 动态颜色：链式紫色 / Realtime 绿色

## 版本
- ielts_tutor: 0.8.0 → 0.9.0

---

## 🔚 22:52 · 日终总结

### ✅ 今日成果
1. **管线2 Qwen-Omni 打通** — API Key / 端点 / sim 连接全部验证通过，双管线架构完整可用
2. **Phase 3/4 并行交付** — 评估引擎增强 + HTML报告 + SQLite持久化 + 成本仪表板
3. **Part 2 长独白模式** — partial STT 逐句更新 + 2s静音倒计时自动交卷
4. **OPC 看板接入** — 实时小任务上报 + 里程碑同步
5. **VAD处理锁修复** — `processing_utt` 并发保护，DeepSeek 处理期间跳过重复 flush
6. **endSession 评估修复** — 从盲等5s改为等 score 消息 + 15s安全兜底

### ❌ 踩坑和教训
1. **API Key 复制错误** — Chat 消息中手打错一个字母(P→Q)，CSV key 才是正确的
2. **端点混淆** — 国际站用 `maas.aliyuncs.com` 而非 `dashscope.aliyuncs.com`
3. **440Hz 正弦波测试失败** — Qwen-Omni 仅支持人声，非人声信号无响应
4. **Kitty inbox 通信故障** — 端口错误(8766 vs 8765)、目录名错误(xiaofeng vs Bryson)
5. **endSession 竞态条件** — 盲等 5s 关 WebSocket，DeepSeek 评估可能未返回就丢分
6. **VAD flush 堆积** — 频繁 flush + 慢处理 = WebSocket 阻塞

### 📊 当前项目状态
- 视频分析视频分析交互Workflow v1.1.2 ✅ 已归档 → 待 Daryl 决定下一步
  → Kitty 在搞 v1.3.1 但仍有 ReactFlow 显示问题
- 视频分析 10fps Vision 管线 ✅ 首测通过 → 待定降本策略
- IELTS 陪练 v2.0: 0.9.0，双管线可用，Part2模式就绪 → 继续调试稳定性
- Agent 体系自进化：调研完成，3个方向待 Daryl 选

### 💡 关键洞察
- Daryl 今天很沮丧，核心诉求不是修 bug 而是看到里程碑式进展
- 两个项目(video_analyzer + ielts_tutor)都有大量 bugfix 但缺乏「哇」的体验
- 下一步应该聚焦产出可演示的完整体验，而非继续修边角
- 失败的经验同样有价值，今天的每个 bug 都是生产级的教训
## 21:15 · Part 2 长独白模式 + VAD处理锁
### 后端
- 新增 `partial` 消息类型：STT 只转写，不触发 DS/TTS
- 返回 `partial_transcript` 供前端实时展示

### 前端
- 处理锁：VAD flush 后锁定，done 后解锁，锁期间语音缓存
- 管线分支：cascade → VAD分段 | realtime → 连续流式 | Part2 → 长独白
- Part 2 长独白模式：每~3s 自动 partial STT → 逐句实时更新
- 2s 静音 → 5秒倒计时覆盖层 → 倒计时内说话取消 → 归零自动交卷
### 根因
- Kitty的inbox在 8765 端口（/api/inbox/xiaofeng），不是废弃的 8766 dispatch
- workspace目录：推到了 xiaofeng/ 但 Kitty 要的是 Bryson/
- inbox有2条未读请求（08:21和09:06），一直没检查

### 已修复
- ✅ 产物正确推送到 opc-workspace/Bryson/ielts_tutor_v2.0/（32个文件）
- ✅ inbox消息已标记resolved
- ✅ 通过 /api/messages 回复Kitty确认完成
- ✅ 确认8765 OPC看板为当前系统，8766已废弃

### 教训
- **OPC看板访问地址**：localhost:8765 API，不是 8766
- **inbox检查**：GET localhost:8765/api/inbox/xiaofeng
- **消息标记**：PATCH localhost:8765/api/inbox/<msgId>
- **主动轮询**：每次session启动应检查inbox有无待处理消息

## 15:50 · 雅思陪练 v2.0 产物推送
- Kitty (OPC看板) 要求推送 v2.0 产物到 opc-workspace/xiaofeng/
- 使用 `rsync` 推送核心文件（backend/server.py, pipeline_qwen_omni.py, session_manager.py, report_generator.py, frontend/index.html, WORKFLOW.md, VERSION）
- Git commit: `4525624` on `detached HEAD`
- 同时推送了 voice-mvp (v1.0) 作为对比参考
