# 2026-08-15 日记

## 👤 用户系统 P1 v1.5.0（Daryl 指令，晚饭期间完成）
- **交付**: backend/auth.py（users+auth_tokens 表 / PBKDF2 200k 迭代 / token 30天可吊销 / 登录20次/10分 + 注册5次/时限流）+ /api/auth/register|login|logout|me + frontend/login.html（登录/注册切换，主题一致）+ /login 路由
- **关键设计**: AUTH_REQUIRED=0 默认 guest —— 现有路由零影响，P2 数据隔离完成后再开强制登录；user_id 字段在 ft_memory/question_progress 已预留
- **验证**: 9 项全过（注册/重复拦截/错密码/登录/me/无token拒绝/登出失效/free_talk照常/login页200）；测试账号已清理
- **成本**: ~$0（无 LLM 调用）；commit 见 git log；VERSION → v1.5.0

## 📚 题库改造 A+B v1.4.1（Daryl 指令，当晚完成）
- **背景**: Daryl 对 IELTS 1/2/3 定式题型麻木 → 商业化方向确认（每月免费额度+按量）→ 先做题库广度
- **交付**:
  - data/question_bank.json: Part1 30话题×5问 + Part2 60卡(6主题×10) + Part3 30主题×5问 = 120 唯一题
  - B 变体: 12 张槽位卡（"Describe {x} you admire" 随机填充）→ ~100 组合
  - backend/question_bank.py: 全局去重（跨主题，修复先选主题后去重的重复 bug）+ 练完轮换 + 异常兜底
  - server.py: 抽题存 session → system prompt 注入种子；Part2 问候语=抽到的题卡；Part3=主题首问
  - question_progress 表（user_id 预留多用户）
- **验证**: 60 连抽无重复 ✅ 轮换 ✅ 30/30 覆盖 ✅ 槽位填充 ✅ 兜底 ✅ LLM 闭环（抽到 sleep 话题→考官自然过渡）✅ 三模式 WS 实测 ✅
- **成本**: 实际 ~$0.05（1 次闭环验证调用），预算 $3 —— 手写种子库省掉 LLM 生成费（自主决策，理由: 质量+成本）
- commit: 7cb6231；台账已更新

## 🧠 Free Talk 持久记忆 v1.4.0（Daryl 决策 + 当晚开发完成）
- **决策链**: Daryl 想给 free talk 做聊天记忆 → 否定多层记忆/触发式回忆（嫌烧 token）→ 采纳"单条持久会话"方案：同一 v4-flash 上下文跨天复用（官方 1M，主动压到 **256K**）→ 超限压缩机制
- **压缩机制**: 原文全量留 SQLite（真相源）；超 230K 软阈值 → 后台把最老一段丢给 LLM 压成摘要（ft_memory 表），上下文 = system+摘要+最近 256K 原文；幂等按 compacted_seq 推进
- **实现**:
  - session_manager.py: ft_memory 表 + get_or_create_ft_session/ft 转录读取/摘要 upsert；add_transcript seq 改以 DB 为准（重启不重号）；get() 加 DB 回退（重启恢复会话）
  - server.py: /api/session/start free_talk 返回固定 sid=ft_main；WS 连接时 _hydrate_ft_history 灌回；free_talk 不截断（其他模式保持 18 条）；user/tutor 转录落库；恢复时 greeting 换成 "Welcome back!"
  - 旧 9 个 free_talk 会话已迁移合并进 ft_main（14 条转录）
- **验证**: py_compile ✅、窗口裁剪/单条超预算边界 ✅、WS 实测 "Welcome back!" 恢复 ✅、IELTS 模式仍随机 sid ✅、VERSION → v1.4.0
- **待办**: 压缩机制未实测触发（需 23 万 token 才触发，属正常）；多用户时 ft_{user_id} 隔离已预留

## 💰 IELTS 陪练 — DeepSeek 成本核算模型费率更新（Daryl 指令）
- **背景**: Daryl 晚间回归（"生理期"结束），指示更新成本核算模型参数 — DeepSeek 8/13 官方公告调价
- **官方新定价**（api-docs.deepseek.com，8/15 抓取确认）:
  - deepseek-v4-flash（本服务在用）: 现行 $0.14/1M in, $0.28/1M out（至 8/16 15:59 UTC）
  - **8/16 16:00 UTC 起峰谷计价**: off-peak $0.22 in / $0.66 out；peak $0.44 in / $1.32 out
  - 峰值时段: 01:00-04:00 与 06:00-10:00 UTC（其余谷值，谷=峰半价）
- **改动文件（3处）**:
  1. `ielts_tutor/backend/server.py` /api/costs — 0.27/1.10（deepseek-chat 旧价）→ ds_rates() 时间感知费率（生效前旧价，生效后峰谷），响应增加 rates 字段
  2. `deepseek_cost_tracker.py` — RATE_INPUT/OUTPUT 常量 + get_ds_rates() 峰谷逻辑，calculate_cost 按当前时段计价
  3. `deepseek_config.yaml` — cost 段增加 peak/offpeak/峰值时段/生效时间参数
- **验证**: py_compile 通过；5 组峰谷边界用例 ALL PASS；launchctl kickstart 重启 8767 服务，/api/costs 返回新 rates（当前仍显示 0.14/0.28 正确，8/16 16:00 UTC 后自动切峰谷）
- **影响提醒**: 输出价最高涨 ~4.7 倍（$0.28→$1.32 peak），晚间练习若落在 UTC 峰值时段（当地 08:00-11:00 / 13:00-17:00 GMT+7）成本上升明显

## 其他
- Daryl 晚间回归，计划继续项目（具体项目待后续对话明确）
