# 2026-06-23 记忆

## Video Analyzer 致命 Bug 修复 ✅
- Daryl 反馈：视频分析管线有三个致命 bug
- 修复内容：
  1. **音频-only 文件**: Playwright 拦截音频流→视频 0 帧 → V2 下载器：逐个 URL 试 + ffprobe 验证视频流
  2. **同名文件从不覆盖**: 旧 `video.mp4` 永远不更新 → URL hash 唯一文件名 + 始终覆盖
  3. **损坏文件仍传入**: 无视频流的文件照样发 Gemini → 保存前 ffprobe 验证 codec_type=video
- `process_video_url` 重构为两阶段循环：Playwright → ffprobe 验证 → 失败则 yt-dlp 兜底
- 端到端测试通过（Douyin 真实 URL: 3.2MB 视频 + Gemini 转写 5 条）

## Self · OPC 看板方法论卡片集成方案
- Self 深入阅读 OPC dashboard 源码（index.html 46KB + server.js 270行）
- 发现 Self 已在系统中注册为 Agent（knowledge management expert）
- 提出方法论卡片接入方案：
  - GET/PATCH /api/methodology-cards 两个新路由
  - Markdown 文件作为唯一数据源（混合模式：文件系统读 + YAML 回写）
  - 前端新增第 6 个 panel
- 等待 Daryl 确认方案方向

## Balance · VAS/IFRS 讨论继续
- Balance 补充 Circular 99 与 FTSE 升级时间对齐分析
- Pillar Two Account 82112 作为 IFRS 趋同硬约束
