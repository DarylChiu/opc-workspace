# 2026-08-08 日记

## ⚠️ Cron 审计自动创建（内容待补充）
- 本日记由 23:59 Cron 自动创建，当日内容未记录
- 需从 session 历史补充实际事件

## 21:11 Daryl DM — IELTS assessment 又出不来了（自由对话无评分）
- **症状**: 刚结束的自由对话（session 2f7ae680）没有出 assessment，AUTO-EVAL score=0
- **根因（日志铁证）**: deepseek-v4-flash 默认开启 **Thinking 模式**（先输出 reasoning_content 再输出 content）
  - 评估调用 max_tokens=600 时 reasoning 吃掉 2473B token，content 只剩 101B 残缺 JSON → json.loads 失败 → score=0 兜底
  - 对话 LLM 延迟 7-10s 也是同一原因（Thinking 模式慢）
  - 附带发现: 主对话/评估/vocab 三处 API 调用都未关闭 Thinking；且 server.py 从未调用 add_transcript → transcripts 从未落库
- **修复** (backend/server.py):
  1. ds_chat / ds_stream / vocab 三处请求体加 `"thinking": {"type": "disabled"}`（官方文档: thinking disabled 后 temperature 才生效）
  2. 重启 LaunchAgent ai.openclaw.ielts，验证评估 content=854B 完整 JSON ✅
- **补数据**: 从 server.err.log 恢复 2f7ae680 的 14 条用户文本 → 补评估（overall=5.5, F5.5/V5.5/G5.0/P6.0, 4 improvements + 3 highlights）→ 落库 + 生成 HTML 报告 + 补 transcripts
- **遗留**: transcripts 落库缺失（server.py 无 add_transcript 调用）— 待 Daryl 决定是否修复
- 教训: DeepSeek v4 系列默认 Thinking 模式，凡要求 JSON 输出的调用必须显式 thinking disabled + 检查 max_tokens 是否被 reasoning 吃掉（已入 lessons.md）
