# Handoff: OPC看板 Workflow编辑器 - 2026-06-19 → 2026-06-20

## 目标
本周完成 OPC看板交互系统 的第一个可用 Demo，当前受阻于 Workflow 编辑器渲染问题。

## 问题概括
Workflow 模块（iframe 加载 workflow-editor.html）在 Daryl 浏览器中白屏不渲染。其他面板（Agent、产物、成本、沙箱）均正常。

## 排查历程

### 尝试过的方案（均失败）
1. ES模块 + importmap 从 esm.sh CDN 加载 → 白屏（无声失败）
2. Vite 构建自包含 Bundle（397KB）→ 仍是 type=module，不加载
3. 完全回退到 git:5805b9c（当初能用的版本）→ 同样白屏

### 结论
浏览器无法可靠加载 ES 模块。即使是最初能用的版本现在也白屏，可能是浏览器更新或网络变化。

### 当前部署（待验证）
**架构**: 普通 `<script>` 标签 + 动态 `import()`
- React/ReactDOM: unpkg CDN UMD 全局变量
- ReactFlow: 动态 `import()` 加载（普通script中可用）
- 加载进度追踪：Step 1-4 逐步显示
- 错误可见：底部红色错误栏
- 文件: `/Users/zhaoyuzhao/WorkBuddy/Claw/opc-dashboard/workflow-editor.html` (624行, 30KB)

### 三个需求状态（代码已完整实现，待渲染验证）
1. 双击节点 → 右侧编辑面板（名称/描述/Agent/触发/时间）
2. 4个方向拖拽手柄（上下左右），白色圆形 12px
3. 连线编辑：点击连线 → 右侧面板切换逻辑类型(THEN/AND/OR/XOR)

## 关键文件
| 文件 | 用途 | 状态 |
|------|------|------|
| `opc-dashboard/index.html` | OPC看板主页面 | ✅ 正常 |
| `opc-dashboard/server.js` | Express 后端 | ✅ 正常 |
| `opc-dashboard/workflow-editor.html` | Workflow 编辑器 | 🔄 待验证 |
| `opc-dashboard/data/workflows.json` | 工作流数据 | ✅ 1个工作流/18节点 |
| `opc-dashboard/workflow-editor.html.bak` | 原始备份(38KB) | 已归档 |

## 隧道
- OPC看板: `cake-marking-metals-miscellaneous.trycloudflare.com` → localhost:8765
- 视频分析: `supreme-hall-blanket-handbook.trycloudflare.com` → localhost:8777

## Git 参考
```bash
git log --oneline -5
# 7e09375 refactor: 普通script标签架构（当前HEAD）
# 0662953 revert: 回退到5805b9c
# 1a415a9 fix: Vite构建React应用
# 35acf9c fix: ES模块import顺序
# 06b76c7 fix: 三个需求+隧道分离
```

## 明天优先事项
1. Daryl 实测普通script方案能否渲染
2. 若能渲染 → 验证三个需求是否正常工作
3. 若不能 → 检查浏览器控制台错误，定位是哪一步失败
4. 备选方案：纯 vanilla JS（不用 React）+ SVG/Canvas 渲染工作流
5. 备选方案：服务端渲染方案

## 注意
- 不要再用 importmap/ES模块方案
- 每次修改后先在本地 `curl http://localhost:8765/` 验证文件可访问
- 隧道不要乱换端口
- 本周Deadline：第一个可用Demo
